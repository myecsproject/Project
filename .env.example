# Environment Setup (Optional)

# Server Port
# Default: 3000
PORT=3000

# Node Environment
# Use 'development' for development
# Use 'production' for production builds
NODE_ENV=development

# Note: This project uses HTTP polling for real-time data (no WebSocket)
